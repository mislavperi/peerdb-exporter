apiVersion: v1
kind: Service
metadata:
  name: {{ include "peerdb_exporter.fullname" . }}
  labels:
    {{- include "peerdb_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "peerdb_exporter.selectorLabels" . | nindent 4 }}
